(function() {
    'use strict';

    angular
        .module('othello')
        .service('AI', function ($http) {

            var decisionTree = [];
            var nodeScores = [];

            this.getBestMove = function(board) {

            };

            function calculateDecisionTree(board) {

            }

            function minimax (board) {
                //return result of minimax algorithm
                return null;
            }
        });

})();

